name: Infra & Deploy

on:
  push:
    branches:
      - main

jobs:
  terraform:
    runs-on: ubuntu-latest
    env:
      TF_WORKING_DIR: terraform
    steps:
      - uses: actions/checkout@v4
      - uses: hashicorp/setup-terraform@v3
      - uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: us-east-1
      - run: terraform -chdir=${{ env.TF_WORKING_DIR }} init
      - run: terraform -chdir=${{ env.TF_WORKING_DIR }} apply -auto-approve

  deploy:
    needs: terraform   # 👈 se ejecuta solo si terraform termina bien
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read
    env:
      PRIMARY_REGION: us-east-1
      SECONDARY_REGION: us-west-2
      CLUSTER_PRIMARY: acme-eks-primary
      CLUSTER_SECONDARY: acme-eks-secondary
    steps:
      - uses: actions/checkout@v4
      - uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: arn:aws:iam::351247743848:role/GitHubOIDCRole
          aws-region: ${{ env.PRIMARY_REGION }}
      - uses: azure/setup-kubectl@v3
        with:
          version: 'latest'
      - run: aws eks update-kubeconfig --region $PRIMARY_REGION --name $CLUSTER_PRIMARY
      - run: kubectl apply -f terraform/nginx-deployment.yaml
